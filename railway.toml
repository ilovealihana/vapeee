[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'alembic upgrade head && uvicorn webapp.main:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
